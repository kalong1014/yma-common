# 缺失与待办

## 当前未完成项

### 1. 全 workspace 质量复核
- [ ] 执行 `cargo check --workspace` 并确保零警告
- [ ] 执行 `cargo clippy --workspace -- -D warnings` 并确保零警告
- [ ] 执行 `cargo test --workspace -- --test-threads=1` 并确保全部通过
- [ ] 全 workspace 单元测试覆盖率 ≥99% 复核

### 2. 文档与版本
- [x] 复核 README.md 中技术栈版本号与 `jishuzhan.md` 完全一致（digest 0.11、sqlx 0.9.0 已对齐）
- [ ] 更新 README.md 变更日志，补充 2026-07-13 迭代内容

### 3. 安全与合规
- [ ] 复核所有 crate 的 `Cargo.toml` 是否仅加载已启用功能对应的依赖
- [ ] 复核敏感配置（如 JWT Secret、数据库密码）是否仅通过环境变量或加密配置注入
