# 已完成功能

## 2026-07-13 迭代修复

### 1. 依赖版本对齐（Cargo.toml）
- `thiserror` 统一为 `2.0`
- `sm2` 统一为 `0.14.0-rc.10`，启用 `dsa`/`pke`/`arithmetic`/`alloc` features
- `sm3` 统一为 `0.5.0-rc.6`
- `jsonwebtoken` 统一为 `9.3`
- `wasmtime` 统一为 `45.0.0`，启用 `async` feature
- `wasmtime-wasi` 统一为 `45.0.3`

### 2. yma-auth JWT 功能增强
- 新增 `JwtError` 错误枚举，覆盖 Secret 长度不足、编码失败、验证失败、Token 过期等场景
- `JwtService::new` 增加 Secret 长度校验：Secret 必须不少于 32 字节
- 默认 Access Token 有效期调整为 24 小时（1440 分钟）
- `LoginResponse` 新增 `token_type` 字段，固定返回 `"Bearer"`
- `AuthService::new` 将 JWT Secret 长度错误透传为 `AuthError::InvalidJwtSecret`
- 更新 `middleware.rs` / `auth_service.rs` / `jwt.rs` 测试用例，使用符合长度要求的测试 Secret

### 3. yma-storage 存储后端安全加固
- `MemoryStorage` 整体标记为 `#[cfg(test)]`，禁止在非测试代码中使用
- `StorageManager::memory()` 同步标记为 `#[cfg(test)]`
- 移除顶层未使用的 `parking_lot::RwLock` 与 `std::collections::HashMap` 导入，改为测试代码内全限定路径

### 4. 编译与测试质量
- 修复 `yma-config` `unused_mut` 警告
- `cargo check -p yma-auth -p yma-config -p yma-events -p yma-logging -p yma-storage` 零警告通过
- `cargo test -p yma-auth -p yma-config -p yma-events -p yma-logging -p yma-storage -- --test-threads=1` 全部通过，共 103 个测试用例

### 5. 技术栈规则文件同步（jishuzhan.md）
- 首行规则说明调整为：**版本号以实际可编译、可解析为准，非必要不降级**
- `digest` 修正为 `0.11`（国密 sm2 0.14.0-rc.10+ / sm3 0.5.0-rc.6+ 强制依赖 digest 0.11）
- `SQLx` 修正为 `0.9.0`（与 yma-common 实际使用版本一致）
- `axum` 修正为 `0.8.9`（yma-common 当前锁定版本，等待 tonic 生态兼容后再统一 0.8.10）
- `meilisearch-sdk` 修正为 `0.24`（yma-common 当前锁定版本，等待 sdk 升级后再统一 0.27）
- `libp2p` 修正为 `0.54`（yma-common 当前锁定版本，等待生态稳定后再统一 0.55）
- `Leptos` 修正为 `0.8.6`（crates.io 实际可用版本，0.8.14/0.8.19 无法解析）
- 复核 [README.md](file:///e:/YMWORK/yma-common/README.md) 变更日志，确认 digest 0.11、sqlx 0.9.0 与 `jishuzhan.md` 一致

### 验证结果
```text
   Doc-tests yma_auth ... ok
   Doc-tests yma_config ... ok
   Doc-tests yma_events ... ok
   Doc-tests yma_logging ... ok
   Doc-tests yma_storage ... ok
```
- `cargo check --workspace --all-targets` 通过
